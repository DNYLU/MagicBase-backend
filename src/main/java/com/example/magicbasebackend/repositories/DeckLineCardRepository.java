package com.example.magicbasebackend.repositories;

import com.example.magicbasebackend.model.DeckLineCard;
import org.springframework.data.repository.CrudRepository;

public interface DeckLineCardRepository extends CrudRepository<DeckLineCard, Long> {
}
